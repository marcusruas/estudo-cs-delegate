using System;
using Delegate.Services;

namespace Delegate
{
    delegate double BinaryNumericOperation(double d, double c);
    class Program
    {
        static void Main(string[] args)
        {
            /*  //Delegate Multcast
            double a = 10;
            double b = 12;

            BinaryNumericOperation op = CalcularService.Max;
            op += CalcularService.Sum;

            System.Console.WriteLine(op(a , b));
            */

            
        }
    }
}
